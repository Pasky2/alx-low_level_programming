#include "main.h"



/**
 * _isdigit - checks for a digit (0 theough 9)
 * @numb: int to be checked
 * Return: 1 if num is a digit, 0 otherwise.
 *
 */

int _isdigit(int numb)
{
	return (numb >= '0' && numb <= '9');
}
