#include "main.h"

/**
 * _isupper - checks for uppercase character
 * @alphabet: the chracter to be tracked
 * Return: 1 if c is uppercase, 0 otherwise
 */



int _isupper(int alphabet)
{
	return (alphabet >= 'A' && alphabet <= 'Z');
}
